请根据已通过检查的 `slide_blueprint.json`，生成《{课程名}》第 {课次} 讲 PPTX。

要求：
1. 使用 `templates/PPT模板.pptx`，如果不存在，则使用简洁高校课程风格。
2. 所有标题、正文、页脚、备注必须是可编辑文本。
3. 表格必须使用 PPT 原生表格。
4. 流程图、架构图、关系图尽量使用 PPT 原生形状、线条、箭头。
5. 不允许把整页做成图片。
6. 不使用来源不明的外部图片。
7. 每页保留 `speaker_notes`。
8. 每页备注区保留 `source_id`。
9. 最后一页添加“参考资料”。
10. 输出 `slide_change_log.md`。
