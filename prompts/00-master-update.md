请进入工作区：

```text
D:\Courseware-AI
```

请按照 `AGENTS.md` 和 `skills/` 中的规则，更新以下课程资源：

课程：{课程名}
范围：{第 X 讲 / 第 X 章 / 全课程}

要求：
1. 先整理来源卡片。
2. 再生成更新计划。
3. 再生成教案、PPT、讲义。
4. 最后运行引用审查。
5. 不覆盖 `input/` 文件。
6. 只使用国内大模型。
7. 不编造来源。
8. 所有事实性内容必须有 `source_id`。
9. 最终成果输出到 `output/` 文件夹。
