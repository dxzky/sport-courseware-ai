# 单门课程运行指令

```markdown
请进入 D:\Courseware-AI，按照 AGENTS.md 和 skills/ 中的规则，更新《{课程名}》全课程。

要求：
1. 按课次逐讲生成，不要一次生成超长全文。
2. 每讲都按以下顺序：来源卡片 → 教案 → PPT 蓝图 → PPTX → 讲义 → 引用审查 → 修复。
3. 所有事实性内容必须有 source_id。
4. 不覆盖 input 文件。
5. 只使用国内大模型。
6. 不编造来源。
7. 最终成果输出到 output 文件夹。
```
