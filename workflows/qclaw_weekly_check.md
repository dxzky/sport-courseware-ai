# 定时检查任务指令

```markdown
请创建一个每周一上午 9 点运行的自动任务。

任务内容：
1. 检查 D:\Courseware-AI\courses\ 三门课程的 input 文件夹是否有新增文件。
2. 如果没有新增文件，只生成一条简短报告。
3. 如果有新增文件，只更新 source_cards 和 source_gap_report。
4. 不自动生成最终教案、PPT、讲义。
5. 把检查结果写入 Courseware-AI/weekly_check_report.md。
6. 如需生成正式成果，必须等待我确认。
```
